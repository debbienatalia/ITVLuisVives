<UserControl x:Class="ITVLuisVives.Front.Views.CitasWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="#E74C3C"/>
                    <Setter Property="BorderThickness" Value="1.5"/>
                    <Setter Property="ToolTip" 
                            Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="📅 Sistema Operativo de Citas — ITV Luis Vives" FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Gestión de inspecciones en tiempo real con validación integrada" FontSize="12" Foreground="#7F8C8D" Margin="0,2,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" 
                      ItemsSource="{Binding Citas}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      CanUserSortColumns="True"
                      Background="White" BorderBrush="#E0E0E0" RowHeight="38"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F0F0F0"
                      AlternatingRowBackground="#F9FAFC" Margin="0,0,20,0">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F1F2F6"/>
                        <Setter Property="Foreground" Value="#2C3E50"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,6"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Fecha/Hora" Binding="{Binding FechaInspeccion, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="130" CanUserSort="True"/>
                    <DataGridTextColumn Header="Matrícula" Binding="{Binding VehiculoMatricula}" Width="100" CanUserSort="True"/>
                    <DataGridTextColumn Header="DNI Propietario" Binding="{Binding Dni}" Width="110" CanUserSort="True"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100" CanUserSort="True"/>
                    <DataGridTextColumn Header="Observaciones" Binding="{Binding Observaciones}" Width="*" CanUserSort="True"/>
                    
                    <DataGridTemplateColumn Header="Acciones" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑️" 
                                        Command="{Binding DataContext.EliminarCitaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Background="#E74C3C" Foreground="White" FontWeight="Bold" Width="30" Height="26" HorizontalAlignment="Center" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="1" Background="#FAFAFA" CornerRadius="6" BorderBrush="#E0E0E0" BorderThickness="1" Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Nueva Inspección" FontSize="16" FontWeight="Bold" Foreground="#34495E" Margin="0,0,0,15"/>

                        <TextBlock Text="DNI Propietario:" FontWeight="SemiBold" Foreground="#7F8C8D"/>
                        <TextBox Text="{Binding Dni, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>

                        <TextBlock Text="Matrícula del Vehículo:" FontWeight="SemiBold" Foreground="#7F8C8D"/>
                        <TextBox Text="{Binding Matricula, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>

                        <TextBlock Text="Observaciones Iniciales:" FontWeight="SemiBold" Foreground="#7F8C8D"/>
                        <TextBox Text="{Binding Observaciones, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

                        <Button Content="Agendar Cita ➕" 
                                Command="{Binding AgregarCitaCommand}"
                                Background="#2E7D32" Foreground="White" FontWeight="SemiBold" Padding="10" Margin="0,10,0,0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>